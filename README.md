Add/Update file in source:
```sh
chezmoi add ~/.bashrc
```
Edit the source state:
```sh
chezmoi edit ~/.bashrc
```
View changes
```sh
chezmoi diff
```
Apply changes
```sh
chezmoi -v apply
```

Dry run `-n`
